import { getRouteInfoFromState } from "expo-router/build/LocationProvider";

const AUTHStack = () => {
    return (
        <view>
            <Text>AUTHStack</Text>
        </view>
    );
};

expect default AUTHStack;